---
phase: 01-scraper-validation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - adapter-service/lib/games.test.js
  - docker-compose.yml
  - adapter-service/scripts/smoke-test.mjs
autonomous: false
requirements:
  - DATA-01
  - DATA-03
  - DATA-05

must_haves:
  truths:
    - "filterGames() returns only games matching kreisId, jugendId, and date range together"
    - "FUSSBALLDE_SAISON is explicitly set in Docker Compose config"
    - "FUSSBALLDE_PAGE_CONCURRENCY and FUSSBALLDE_MATCH_CONCURRENCY are set to 3 in Docker Compose"
    - "Full-week scraper run completes without HTTP 429 errors"
  artifacts:
    - path: "adapter-service/lib/games.test.js"
      provides: "Extended filter tests covering combined kreisId+jugendId+dateRange filtering"
      contains: "filterGames"
    - path: "docker-compose.yml"
      provides: "Docker Compose with explicit FUSSBALLDE_SAISON and concurrency env vars"
      contains: "FUSSBALLDE_SAISON"
  key_links:
    - from: "docker-compose.yml"
      to: "adapter-service/scripts/fetch-week.fussballde.mjs"
      via: "environment variables"
      pattern: "FUSSBALLDE_SAISON|FUSSBALLDE_PAGE_CONCURRENCY|FUSSBALLDE_MATCH_CONCURRENCY"
    - from: "adapter-service/lib/games.js"
      to: "adapter-service/lib/games.test.js"
      via: "filterGames import"
      pattern: "filterGames"
---

<objective>
Validate date-range filtering end-to-end, lock down the season config in Docker Compose, and run a full-week smoke test to confirm zero HTTP 429 errors with hardened concurrency.

Purpose: DATA-03 requires filtering by Kreis + Jugendklasse + date range. DATA-05 requires production-safe concurrency. DATA-01 requires the full pipeline to work end-to-end. This plan closes the phase by validating everything together.
Output: Extended filter tests, Docker Compose env config, successful full-week scraper run
</objective>

<execution_context>
@/Users/playboiiboggos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/playboiiboggos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scraper-validation/01-RESEARCH.md
@.planning/phases/01-scraper-validation/01-01-SUMMARY.md
@.planning/phases/01-scraper-validation/01-02-SUMMARY.md
@adapter-service/lib/games.js
@adapter-service/lib/games.test.js
@docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend filter tests and lock season config in Docker Compose</name>
  <files>adapter-service/lib/games.test.js, docker-compose.yml</files>
  <action>
**Part A: Extend filterGames tests in `adapter-service/lib/games.test.js`**

Add these test cases to the existing `describe("adapter games lib", ...)` block:

1. "filters by combined kreisId + jugendId + date range":
   - Create 4 games: one matching all criteria, one wrong kreisId, one wrong jugendId, one outside date range
   - Call `filterGames(games, { kreisId: "moenchen", jugendId: "d-jugend", fromDate: "2026-04-05", toDate: "2026-04-06" })`
   - Assert exactly 1 game returned and it is the matching one

2. "returns all games when no filter criteria provided":
   - Create 2 games with different kreisId/jugendId
   - Call `filterGames(games, {})` (empty payload)
   - Assert both games returned (filter is a no-op when criteria are empty)

3. "handles games with empty kreisId gracefully":
   - Create a game with `kreisId: ""` and a game with `kreisId: "moenchen"`
   - Call `filterGames(games, { kreisId: "moenchen" })`
   - Verify behavior matches existing code (games with empty kreisId pass through -- this is the documented behavior from RESEARCH.md Pitfall 4)

**Part B: Add scraper env vars to `docker-compose.yml`**

Read the existing `docker-compose.yml`. Find the adapter service definition. Add these environment variables to the adapter service (in the `environment:` section, or create one if it does not exist):

```yaml
FUSSBALLDE_SAISON: "2526"
FUSSBALLDE_PAGE_CONCURRENCY: "3"
FUSSBALLDE_MATCH_CONCURRENCY: "3"
```

Do NOT change any other Docker Compose configuration. Preserve all existing services, profiles, volumes, and ports.
  </action>
  <verify>
    <automated>cd /Users/playboiiboggos/.openclaw/workspace/ScoutX && npm test -- --reporter=verbose 2>&1 | tail -40</automated>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>All tests pass including new combined filter test. docker-compose.yml contains FUSSBALLDE_SAISON, PAGE_CONCURRENCY=3, MATCH_CONCURRENCY=3 for the adapter service.</done>
</task>

<task type="auto">
  <name>Task 2: Run full-week smoke test and validate no 429 errors</name>
  <files>adapter-service/scripts/smoke-test.mjs</files>
  <action>
Update `adapter-service/scripts/smoke-test.mjs` to add a `--full-week` flag that runs a broader date range (Monday through Sunday of the current or next week) instead of just the weekend.

When `--full-week` is passed as a CLI argument (`process.argv.includes("--full-week")`):
- Set `SCOUTPLAN_FROM_DATE` to the Monday of the upcoming week
- Set `SCOUTPLAN_TO_DATE` to the Sunday of the upcoming week
- Keep all other env vars the same (FUSSBALLDE_PAGE_CONCURRENCY=2, FUSSBALLDE_MATCH_CONCURRENCY=2, FUSSBALLDE_SAISON=2526)
- Increase subprocess timeout to 180 seconds (full week fetches more data)

After the subprocess completes, add an additional assertion:
- Parse stderr for any lines containing "HTTP 429" or "429" -- assert there are zero occurrences
- Log the total number of games returned and the time elapsed

The default behavior (no flag) should remain unchanged from plan 01-01.

Run the smoke test with `--full-week` to validate:
1. The hardened concurrency (max 3 from plan 01-02) prevents 429 errors across a full week of data
2. Games from multiple days are returned
3. All field assertions still pass
  </action>
  <verify>
    <automated>node adapter-service/scripts/smoke-test.mjs --full-week 2>&1 | tail -20</automated>
    <manual>Check that stderr contains zero "429" lines and output shows games across multiple dates</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>Full-week smoke test exits 0, zero HTTP 429 errors, games returned across multiple dates, all field assertions pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase 1 final verification</name>
  <what-built>Complete Phase 1 scraper validation: live smoke test passing, concurrency hardened to max 3 with jitter, live HTML fixture tests, date-range filter tests, season config locked in Docker Compose, full-week run with no 429 errors.</what-built>
  <how-to-verify>
    1. Run `npm test` -- all tests should pass (existing 30+ plus new fixture and filter tests)
    2. Run `node adapter-service/scripts/smoke-test.mjs` -- should exit 0 with real game data
    3. Check `docker-compose.yml` -- adapter service should have FUSSBALLDE_SAISON=2526, PAGE_CONCURRENCY=3, MATCH_CONCURRENCY=3
    4. Verify the full-week run output shows games from multiple dates with real venues
    5. Confirm no HTTP 429 warnings in any run's stderr output
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 1, or describe remaining issues</resume-signal>
</task>

</tasks>

<verification>
- `npm test` passes all tests (existing + new filter tests)
- `docker-compose.yml` contains all three FUSSBALLDE env vars for adapter service
- Full-week smoke test exits 0 with zero 429 errors
- Games span multiple dates in full-week output
- Phase success criteria from ROADMAP all met:
  1. Real games with all fields populated
  2. No HTTP 429 during full-week fetch
  3. Filtering by Kreis, Jugendklasse, date range works
  4. FUSSBALLDE_SAISON explicitly set in config
</verification>

<success_criteria>
Phase 1 is complete: the scraper reliably returns real game data from fussball.de with venue/address, respects a 3-concurrent-request ceiling with jitter, filters correctly by Kreis/Jugendklasse/date range, and the season is explicitly locked in Docker Compose config.
</success_criteria>

<output>
After completion, create `.planning/phases/01-scraper-validation/01-03-SUMMARY.md`
</output>
